  marks=int(input("Enter your marks:"))
if marks >= 90:
  print("grade A")
  elif marks >= 80:
    print("grade b"0)
    elif marks >= 70:
      print("grade c")
      elif marks >= 60:
        print ("grade d")
       else:
        print("fail.") 
